# Pandas dataframe incorrectly including data type in output
all_regressors['sooner'].append(delays_sooner['ResponseTime'].values) all_regressors['safer'].append(probs_safer['ResponseTime'].values) all_regressors['later'].append(delays_later['ResponseTime'].values) all_regressors['risky'].append(probs_risky['ResponseTime'].values)
