# Better approach to build graph from list clustering related items by two properties
&gt;&gt;&gt; G.add_node('a') # adds node 'a'
&gt;&gt;&gt; G.add_edge('b','c') # adds edge 'b'-'c' (and also nodes 'b', 'c')
