# Can I persist any of the variables within a Python any() statement?
predRange = [x for x in range(0,len(myList)) if predicate(x)]
 if len(predRange) &gt; 0:
   #hey look, it's already persisted in predRange!
