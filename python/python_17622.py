# Python 2.7 Unicode Error within a function (using __future__ print_function and unicode_literals)
Apache can't load utf8 python file
Reshape a 1D array to a masked 2D array
f_values = some_function_of(indgrid[mask])
result = np.zeros(indgrid.shape)
result[mask] = f_values
