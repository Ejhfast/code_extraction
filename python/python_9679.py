# Why is there a python "KeyError:"error appearing sometimes and then not other times when returning data from Twitter-api?
tweet.get('location', '&lt;unknown location&gt;').encode('ascii', 'ignore')
