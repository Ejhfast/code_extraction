# Multiple Ttk Entry Widgets Causing Syntax Error
start_entry.grid(column=1, row=1, sticky=(E, W)
#                                              ^
end_entry = ttk.Entry(mainframe, textvariable = end)
