# Algorithm to search regex pattern stuck for some txt
Python: How to check the value of a non-existent list element without getting IndexError?
x = 'foo' if len(myList) &gt; 2 and myList[2] is not None else 'bar'
